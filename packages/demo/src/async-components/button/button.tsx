import React from 'react';
import { Button } from '@tarojs/components';

import './button.less';

const AsyncButton = () => {
  return <Button className="async-button-component">Async Button</Button>;
};

export default AsyncButton;
